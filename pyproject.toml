[project]
name = "memforensics-mcp"
version = "0.1.0"
description = "内存取证 MCP 服务 - 基于 MemProcFS"
requires-python = ">=3.10"
dependencies = [
    "mcp>=0.1.0",
    "memprocfs>=5.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

# Python 解释器通过 mcp.json 的 --python 参数指定
