# Memforensics-MCP

 基于python的内存取证mcp服务，适配cursor windsurf等有访问文件功能的agent使用，需要将 `output`文件夹打开至工作区供agent读取，借鉴了[**[LovelyMem](https://github.com/Tokeii0/LovelyMem)**](https://github.com/Tokeii0/LovelyMem)

## mcp.json

```json
{
  "mcpServers": {
    "memforensics": {
      "command": "uv",
      "args": [
        "run",
        "F:\\Path\\To\\Your\\Project\\MemForensics\\server.py"
      ],
      "env": {}
    }
  }
}
```


## 使用方法
由于`mem` `vol2` `vol3`有很多功能，以及还有很多插件，过多的工具会影响agent执行任务的能力，因此设计了搜索工具，可以在`skill`或者提示词中告诉ai先按需求搜索对应的工具再执行

## 工具列表 (共182个)

| 引擎         | 工具数量 |
| ------------ | -------- |
| 搜索工具     | 6 个     |
| MemProcFS    | 36 个    |
| Volatility 2 | 77 个    |
| Volatility 3 | 63 个    |

---

### 搜索与帮助工具 (6个) - AI入口点

| 工具名称                   | 功能描述                                                         |
| -------------------------- | ---------------------------------------------------------------- |
| `search_tools`           | 🔍 内存取证工具搜索 (AI应首先调用)，通过关键词搜索合适的取证工具 |
| `list_tools_by_category` | 按分类列出工具，如 "进程分析"、"凭据提取"                        |
| `list_tools_by_engine`   | 按引擎列出工具 (MemProcFS / Volatility2 / Volatility3)           |
| `get_tool_info`          | 获取单个工具的详细信息                                           |
| `get_unique_features`    | 获取各引擎独有功能                                               |
| `forensics_help`         | 获取内存取证 MCP 服务帮助信息                                    |

---

### MemProcFS 工具 (36个)

#### 系统信息 (5个)

| 工具名称             | 功能描述                                        |
| -------------------- | ----------------------------------------------- |
| `mem_info`         | 获取内存镜像基本信息 (镜像大小、架构、OS版本等) |
| `mem_sysinfo`      | 获取系统详细信息 (计算机名、域、OS版本等)       |
| `mem_users`        | 获取用户账户列表                                |
| `mem_dtb`          | 获取页表基址(DTB)信息                           |
| `mem_certificates` | 获取系统证书列表                                |

#### 进程分析 (9个)

| 工具名称              | 功能描述                                     |
| --------------------- | -------------------------------------------- |
| `mem_pslist`        | 获取进程列表 (PID, 名称, PPID, 路径, 命令行) |
| `mem_pstree`        | 获取进程树结构                               |
| `mem_handles`       | 获取进程句柄列表                             |
| `mem_modules`       | 获取进程加载模块/DLL                         |
| `mem_vad`           | 获取进程VAD信息 (Virtual Address Descriptor) |
| `mem_threads`       | 获取进程线程列表                             |
| `mem_heap`          | 获取进程堆信息                               |
| `mem_console`       | 获取控制台输出内容                           |
| `mem_procdump_hash` | 获取进程可执行文件哈希值 (MD5/SHA1/SHA256)   |

#### 网络分析 (2个)

| 工具名称                 | 功能描述                                                |
| ------------------------ | ------------------------------------------------------- |
| `mem_netstat`          | 获取网络连接状态 (协议、本地地址、远程地址、状态、进程) |
| `mem_netstat_timeline` | 获取网络活动时间线                                      |

#### 文件系统 (3个)

| 工具名称              | 功能描述                 |
| --------------------- | ------------------------ |
| `mem_filescan`      | 获取内存中的文件对象列表 |
| `mem_ntfs_timeline` | 获取NTFS文件时间线       |
| `mem_dumpfile`      | 从内存中提取文件         |

#### 注册表分析 (6个)

| 工具名称                   | 功能描述             |
| -------------------------- | -------------------- |
| `mem_hivelist`           | 获取注册表Hive列表   |
| `mem_printkey`           | 读取注册表键值       |
| `mem_autoruns`           | 获取自启动项         |
| `mem_usb_devices`        | 获取USB设备历史      |
| `mem_network_interfaces` | 获取网络接口配置     |
| `mem_reg_timeline`       | 获取注册表修改时间线 |

#### 服务与驱动 (4个)

| 工具名称              | 功能描述         |
| --------------------- | ---------------- |
| `mem_services`      | 获取系统服务列表 |
| `mem_drivers`       | 获取驱动程序列表 |
| `mem_tasks`         | 获取计划任务     |
| `mem_driver_detail` | 获取驱动详细信息 |

#### 凭据提取 (3个)

| 工具名称                 | 功能描述                                         |
| ------------------------ | ------------------------------------------------ |
| `mem_pypykatz`         | 使用 pypykatz 提取 lsass 凭据 (密码、哈希、票据) |
| `mem_regsecrets`       | 从注册表提取凭据 (SAM/LSA/SECURITY)              |
| `mem_kerberos_tickets` | 导出 Kerberos 票据                               |

#### 恶意检测 (2个)

| 工具名称         | 功能描述                |
| ---------------- | ----------------------- |
| `mem_findevil` | 综合恶意检测 (FindEvil) |
| `mem_yara`     | YARA 规则扫描结果       |

#### 时间线 (2个)

| 工具名称                 | 功能描述                                    |
| ------------------------ | ------------------------------------------- |
| `mem_timeline_all`     | 获取综合时间线 (进程、网络、文件、注册表等) |
| `mem_timeline_process` | 获取进程创建时间线                          |

---

### Volatility 2 工具 (77个)

#### 系统信息 (6个)

| 工具名称              | 功能描述                       |
| --------------------- | ------------------------------ |
| `vol2_imageinfo`    | 获取镜像信息并自动检测 Profile |
| `vol2_kdbgscan`     | 扫描 KDBG 结构                 |
| `vol2_shutdowntime` | 获取系统关机时间               |
| `vol2_envars`       | 获取环境变量                   |
| `vol2_verinfo`      | 获取 PE 文件版本信息           |
| `vol2_auditpol`     | 获取审计策略                   |

#### 进程分析 (12个)

| 工具名称          | 功能描述                          |
| ----------------- | --------------------------------- |
| `vol2_pslist`   | 获取进程列表 (通过 EPROCESS 链表) |
| `vol2_psscan`   | 扫描进程 (包括隐藏/已终止进程)    |
| `vol2_pstree`   | 获取进程树                        |
| `vol2_psxview`  | 多视图隐藏进程检测                |
| `vol2_cmdline`  | 获取进程命令行参数                |
| `vol2_cmdscan`  | 扫描 CMD 历史命令                 |
| `vol2_consoles` | 获取控制台输出内容                |
| `vol2_dlllist`  | 获取进程加载的 DLL 列表           |
| `vol2_handles`  | 获取进程句柄                      |
| `vol2_getsids`  | 获取进程 SID (安全标识符)         |
| `vol2_privs`    | 获取进程权限                      |
| `vol2_vadinfo`  | 获取进程 VAD 信息                 |

#### 网络分析 (2个)

| 工具名称             | 功能描述                    |
| -------------------- | --------------------------- |
| `vol2_netscan`     | 网络连接扫描 (Vista/7/8/10) |
| `vol2_connections` | 网络连接 (Windows XP)       |

#### 文件系统 (4个)

| 工具名称             | 功能描述            |
| -------------------- | ------------------- |
| `vol2_filescan`    | 文件对象扫描        |
| `vol2_mftparser`   | MFT (主文件表) 解析 |
| `vol2_symlinkscan` | 符号链接扫描        |
| `vol2_dumpfiles`   | 从内存导出文件      |

#### 注册表分析 (8个)

| 工具名称              | 功能描述                            |
| --------------------- | ----------------------------------- |
| `vol2_hivelist`     | 获取注册表 Hive 列表                |
| `vol2_printkey`     | 打印注册表键值                      |
| `vol2_hivedump`     | Hive 结构转储                       |
| `vol2_dumpregistry` | 导出全部注册表 Hive 文件            |
| `vol2_userassist`   | 获取 UserAssist 程序执行记录        |
| `vol2_shellbags`    | ShellBags 分析 (文件夹访问记录)     |
| `vol2_shimcache`    | 获取 Shimcache (应用程序兼容性缓存) |
| `vol2_autoruns`     | 获取自启动项 (需要社区插件)         |

#### 凭据提取 (4个) ⭐独有mimikatz

| 工具名称           | 功能描述                                         |
| ------------------ | ------------------------------------------------ |
| `vol2_hashdump`  | 提取 SAM 密码哈希 (LM/NT Hash)                   |
| `vol2_lsadump`   | 提取 LSA Secrets                                 |
| `vol2_cachedump` | 提取域缓存凭据 (DCC/DCC2)                        |
| `vol2_mimikatz`  | ⭐ Mimikatz 凭据提取 (Vol2 独有，可提取明文密码) |

#### 恶意检测 (6个)

| 工具名称              | 功能描述                                               |
| --------------------- | ------------------------------------------------------ |
| `vol2_malfind`      | 代码注入检测 (检测 RWX 权限且包含 PE 头的可疑内存区域) |
| `vol2_malfinddeep`  | 深度恶意代码检测 (扩展插件)                            |
| `vol2_apihooks`     | API 钩子检测 (IAT, EAT, Inline hooks)                  |
| `vol2_apihooksdeep` | 深度 API 钩子检测 (扩展插件)                           |
| `vol2_ldrmodules`   | 隐藏 DLL 检测 (LDR 模块交叉检查)                       |
| `vol2_hollowfind`   | 进程镂空检测 (社区插件)                                |

#### 内核分析 (10个)

| 工具名称                 | 功能描述                                     |
| ------------------------ | -------------------------------------------- |
| `vol2_modules`         | 获取内核模块列表                             |
| `vol2_modscan`         | 扫描内核模块 (包括已卸载)                    |
| `vol2_driverscan`      | 扫描驱动对象                                 |
| `vol2_driverirp`       | 获取驱动 IRP 钩子                            |
| `vol2_ssdt`            | 获取 SSDT (系统服务描述符表)，检测 SSDT 钩子 |
| `vol2_callbacks`       | 获取内核回调函数                             |
| `vol2_timers`          | 获取内核定时器                               |
| `vol2_unloadedmodules` | 获取已卸载的内核模块                         |
| `vol2_devicetree`      | 获取设备树                                   |
| `vol2_getservicesids`  | 获取服务 SID                                 |

#### 服务分析 (1个)

| 工具名称         | 功能描述 |
| ---------------- | -------- |
| `vol2_svcscan` | 服务扫描 |

#### GUI/窗口分析 (7个) ⭐Vol2独有

| 工具名称              | 功能描述          |
| --------------------- | ----------------- |
| `vol2_windows`      | ⭐ 获取窗口信息   |
| `vol2_wintree`      | ⭐ 获取窗口树     |
| `vol2_deskscan`     | ⭐ 桌面扫描       |
| `vol2_screenshot`   | ⭐ 截图重建       |
| `vol2_clipboard`    | ⭐ 获取剪贴板内容 |
| `vol2_messagehooks` | ⭐ 消息钩子检测   |
| `vol2_eventhooks`   | ⭐ 事件钩子检测   |

#### 浏览器痕迹 (5个) ⭐Vol2独有

| 工具名称                | 功能描述            |
| ----------------------- | ------------------- |
| `vol2_iehistory`      | ⭐ IE 浏览历史记录  |
| `vol2_chromehistory`  | ⭐ Chrome 浏览历史  |
| `vol2_firefoxhistory` | ⭐ Firefox 浏览历史 |
| `vol2_trustrecords`   | ⭐ Office 信任记录  |
| `vol2_prefetch`       | ⭐ 预读文件分析     |

#### 加密分析 (3个) ⭐Vol2独有

| 工具名称                  | 功能描述              |
| ------------------------- | --------------------- |
| `vol2_bitlocker`        | ⭐ BitLocker 密钥提取 |
| `vol2_truecryptsummary` | ⭐ TrueCrypt 摘要     |
| `vol2_truecryptmaster`  | ⭐ TrueCrypt 主密钥   |

#### 其他分析 (5个)

| 工具名称            | 功能描述     |
| ------------------- | ------------ |
| `vol2_timeliner`  | 综合时间线   |
| `vol2_mutantscan` | 互斥对象扫描 |
| `vol2_atomscan`   | 原子表扫描   |
| `vol2_sessions`   | 会话信息     |
| `vol2_bigpools`   | 大内存池     |

#### 数据导出 (4个)

| 工具名称          | 功能描述           |
| ----------------- | ------------------ |
| `vol2_procdump` | 导出进程可执行文件 |
| `vol2_memdump`  | 导出进程完整内存   |
| `vol2_dlldump`  | 导出 DLL           |
| `vol2_vaddump`  | 导出 VAD 区域      |

---

### Volatility 3 工具 (63个)

#### 系统信息 (4个)

| 工具名称           | 功能描述         |
| ------------------ | ---------------- |
| `vol3_info`      | 获取系统信息     |
| `vol3_crashinfo` | 获取崩溃转储信息 |
| `vol3_verinfo`   | 获取版本信息     |
| `vol3_envars`    | 获取环境变量     |

#### 进程分析 (14个)

| 工具名称            | 功能描述       |
| ------------------- | -------------- |
| `vol3_pslist`     | 获取进程列表   |
| `vol3_psscan`     | 进程扫描       |
| `vol3_pstree`     | 进程树         |
| `vol3_psxview`    | 跨视图进程检测 |
| `vol3_cmdline`    | 获取命令行参数 |
| `vol3_dlllist`    | 获取 DLL 列表  |
| `vol3_handles`    | 获取句柄       |
| `vol3_getsids`    | 获取 SID 列表  |
| `vol3_privileges` | 获取权限       |
| `vol3_ldrmodules` | LDR 模块检测   |
| `vol3_vadinfo`    | VAD 信息       |
| `vol3_vadwalk`    | VAD 遍历       |
| `vol3_sessions`   | 会话信息       |
| `vol3_joblinks`   | 作业对象       |

#### 线程分析 (4个) ⭐Vol3独有

| 工具名称                    | 功能描述        |
| --------------------------- | --------------- |
| `vol3_threads`            | 线程列表        |
| `vol3_thrdscan`           | 线程扫描        |
| `vol3_suspicious_threads` | ⭐ 可疑线程检测 |
| `vol3_suspended_threads`  | ⭐ 挂起线程检测 |

#### 网络分析 (2个)

| 工具名称         | 功能描述 |
| ---------------- | -------- |
| `vol3_netscan` | 网络扫描 |
| `vol3_netstat` | 网络状态 |

#### 文件系统 (2个)

| 工具名称           | 功能描述 |
| ------------------ | -------- |
| `vol3_filescan`  | 文件扫描 |
| `vol3_dumpfiles` | 文件导出 |

#### 注册表分析 (5个)

| 工具名称              | 功能描述        |
| --------------------- | --------------- |
| `vol3_hivelist`     | Hive 列表       |
| `vol3_hivescan`     | Hive 扫描       |
| `vol3_printkey`     | 读取键值        |
| `vol3_userassist`   | UserAssist 记录 |
| `vol3_certificates` | 获取证书信息    |

#### 凭据提取 (3个)

| 工具名称           | 功能描述              |
| ------------------ | --------------------- |
| `vol3_hashdump`  | 提取 SAM 哈希         |
| `vol3_lsadump`   | 提取 LSA              |
| `vol3_cachedump` | 提取域缓存凭据 (DCC2) |

#### 恶意检测 (6个) ⭐最强

| 工具名称                   | 功能描述                           |
| -------------------------- | ---------------------------------- |
| `vol3_malfind`           | 代码注入检测                       |
| `vol3_hollowprocesses`   | ⭐ 进程镂空检测                    |
| `vol3_processghosting`   | ⭐ 进程幽灵检测                    |
| `vol3_skeleton_key`      | ⭐ 骨架密钥检测                    |
| `vol3_direct_syscalls`   | ⭐ 直接系统调用检测 (EDR 逃逸技术) |
| `vol3_indirect_syscalls` | ⭐ 间接系统调用检测                |

#### 内核分析 (9个)

| 工具名称              | 功能描述     |
| --------------------- | ------------ |
| `vol3_modules`      | 内核模块列表 |
| `vol3_modscan`      | 模块扫描     |
| `vol3_driverscan`   | 驱动扫描     |
| `vol3_drivermodule` | 驱动模块信息 |
| `vol3_driverirp`    | 获取驱动 IRP |
| `vol3_ssdt`         | SSDT 表      |
| `vol3_callbacks`    | 获取内核回调 |
| `vol3_timers`       | 定时器       |
| `vol3_devicetree`   | 获取设备树   |

#### 服务分析 (3个)

| 工具名称                | 功能描述     |
| ----------------------- | ------------ |
| `vol3_getservicesids` | 服务 SID     |
| `vol3_svclist`        | 服务列表     |
| `vol3_svcdiff`        | 服务差异对比 |

#### 内存池 (2个)

| 工具名称             | 功能描述     |
| -------------------- | ------------ |
| `vol3_bigpools`    | 获取大内存池 |
| `vol3_poolscanner` | 池扫描       |

#### 其他分析 (6个)

| 工具名称              | 功能描述       |
| --------------------- | -------------- |
| `vol3_strings`      | 字符串搜索     |
| `vol3_symlinkscan`  | 符号链接扫描   |
| `vol3_mutantscan`   | 互斥对象扫描   |
| `vol3_mbrscan`      | MBR 扫描       |
| `vol3_shimcachemem` | Shimcache 分析 |
| `vol3_iat`          | 导入表分析     |

#### 数据导出 (3个)

| 工具名称          | 功能描述     |
| ----------------- | ------------ |
| `vol3_procdump` | 进程导出     |
| `vol3_memmap`   | 内存映射导出 |
| `vol3_pedump`   | PE 文件导出  |

---

## 引擎特色功能

### MemProcFS 独有

- 实时文件系统虚拟化
- FindEvil 综合恶意检测
- pypykatz 集成凭据提取
- 快速文件导出

### Volatility 2 独有

- Mimikatz 明文密码提取
- GUI/桌面分析 (窗口、截图、剪贴板)
- 浏览器历史记录 (IE/Chrome/Firefox)
- 加密工具密钥提取 (BitLocker/TrueCrypt)
- 丰富的社区插件支持

### Volatility 3 独有

- 直接/间接系统调用检测 (EDR 逃逸技术检测)
- 进程幽灵检测 (ProcessGhosting)
- 骨架密钥检测 (Skeleton Key)
- 可疑/挂起线程分析
- 现代 Windows 版本支持
- 更好的性能和稳定性
